import { Spectral } from "next/font/google";

export const type_display = Spectral({
  weight: ["400", "700"],
  subsets: ["latin"],
  style: "normal",
  display: "swap",
  variable: "--type-display",
});
